{
  "$comment": "Creature-state thresholds on the SMOOTHED burn rate (tokens/min). The signal includes cache by default (settings.rateCacheWeight=1.0), so it runs ~70x larger than work-only; these cutoffs are calibrated for that cache-inclusive scale (avg cache ~118k/turn measured). If you set rateCacheWeight=0 (work only), divide these by ~75 (working ~1500, stressed ~8000, onfire ~25000). Each state has separate up/down cutoffs (hysteresis) so a noisy signal does not strobe. Tune freely — read live in dev, no rebuild needed.",
  "states": {
    "sleeping": { "$note": "entered when no active block; not rate-driven" },
    "calm":     { "up": 0,       "down": 0 },
    "working":  { "up": 110000,  "down": 60000 },
    "stressed": { "up": 600000,  "down": 410000 },
    "onfire":   { "up": 1850000, "down": 1330000 }
  },
  "onfire": {
    "$note": "onfire requires the high rate to be sustained, not a single spike",
    "sustainedSeconds": 12
  },
  "spent": {
    "$note": "spent = the crash AFTER burning hot: when the rate collapses below rateThreshold within afterOnfireSeconds of being onfire, the rat is spent for durationSeconds (then relaxes to calm). NOT tied to quota exhaustion.",
    "rateThreshold": 110000,
    "afterOnfireSeconds": 90,
    "durationSeconds": 20
  },
  "spike": {
    "$note": "instant-rate jump (tokens/min) above this triggers a transient flinch bounce (cache-inclusive scale; ~80k on work-only)",
    "instantTpmFlinch": 6000000
  },
  "quota": {
    "$note": "Layer 2 — near-limit overlay ramp. The overlay opacity rises linearly from 0 to 1 as consumedWithCache/ceiling climbs from startPercent to fullPercent (the readout shows a % in this band, then a refresh countdown at/over the ceiling). The ceiling auto-calibrates to your largest recent completed block (see settings.default.json limitHistoryDays / limitMinCredibleTokens; planLimits can override it).",
    "startPercent": 0.90,
    "fullPercent": 0.99
  },
  "events": {
    "$note": "Layer 3 — transient one-shot events. `priority` resolves which single event plays when several fire at once (highest first). The cooldowns keep a retryable hiccup from spamming an event; flinch is gated by the spike detector so it needs none.",
    "priority": ["error", "refreshed", "flinch"],
    "errorDebounceSeconds": 30,
    "refreshedCooldownSeconds": 60
  },
  "$activityFloorNote": "Fast-attack perk-up: if a token was written within this many seconds, the rat is at least 'working' immediately (don't wait for the smoothed rate to climb). Higher states still come from the smoothed rate.",
  "activityFloorSeconds": 15,
  "$idleTimeoutNote": "Smart napping: the nap clock runs from the last conversational line (user or assistant), so a new message resets it; 'done' (an open question / finished turn) never naps until done_hold elapses. This is the hold time (seconds) after the last activity before the rat sleeps.",
  "idleTimeoutSeconds": 60,
  "$doneHoldNote": "How long to hold the 'done' pose after a finished turn before the rat naps. 120 = 2 minutes.",
  "doneHoldSeconds": 120,
  "$sentHoldNote": "After the user sends a message we're awaiting Claude. Hold the 'thinking' pose this long before napping — longer than idleTimeoutSeconds so the rat doesn't nap through the 'dead air' before Claude starts responding. 180 = 3 minutes.",
  "sentHoldSeconds": 180
}
